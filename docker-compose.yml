services:
  flexproof:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    volumes:
      - flexproof-data:/app/data

volumes:
  flexproof-data:
